/**
 * Main
 *
 * A very small class that just starts the StudentApp. I kept this file so the
 * project can still use the "Main" entry point in tasks or launch configs.
 */
public class Main {
    public static void main(String[] args) {
        StudentApp.main(args);
    }
}
